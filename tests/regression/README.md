# Regression tests

Long-lived behavioral regression tests live in `apps/api/app/tests` and `apps/web/src/**/*.test.*`.
The canonical runner executes them together with the API contract coverage checks.
