# ADR-003: Local authentication with future OIDC

Status: Accepted. The first release uses hashed passwords plus short-lived JWT access and revocable refresh tokens. Identity boundaries permit a future Microsoft Entra ID OIDC provider.
