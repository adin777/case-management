import React,{useMemo} from 'react';import ReactDOM from 'react-dom/client';import {BrowserRouter} from 'react-router-dom';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {CssBaseline,ThemeProvider} from '@mui/material';import {useTranslation} from 'react-i18next';import App from './App';import './styles.css';import {createAppTheme} from './theme';import './i18n';
const client=new QueryClient();
function Root(){const{i18n}=useTranslation();const direction=i18n.language==='en'?'ltr':'rtl';const theme=useMemo(()=>createAppTheme(direction),[direction]);return <ThemeProvider theme={theme}><CssBaseline/><QueryClientProvider client={client}><BrowserRouter><App/></BrowserRouter></QueryClientProvider></ThemeProvider>}
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><Root/></React.StrictMode>);
