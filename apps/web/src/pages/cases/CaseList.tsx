import {useNavigate} from 'react-router-dom';import {Chip,Paper,Stack,Typography} from '@mui/material';import type {Case} from '../../types';import {statusLabel} from '../../status';
export function CaseList({cases}:{cases:Case[]}){const nav=useNavigate();return <Stack spacing={1.5}>{cases.map(item=><Paper className="case-row" key={item.id} onClick={()=>nav('/cases/'+item.id)} sx={{p:2,cursor:'pointer'}}><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between"><div><Typography fontWeight={800}>{item.title}</Typography><Typography variant="body2" color="text.secondary">{item.case_number} · {new Date(item.created_at).toLocaleDateString('he-IL')}</Typography></div><Stack direction="row" spacing={1}><Chip label={statusLabel(item.status)} color="primary" variant="outlined"/><Chip label={item.priority}/></Stack></Stack></Paper>)}</Stack>}
