import { useState } from 'react';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { Button, Chip, MenuItem, Paper, Stack, TextField, Typography } from '@mui/material';
import { api } from '../../../../api/client';
import type { Environment, User } from '../../../../types';
type Membership = { id:string; user_name:string; source:'manual'|'rule' };
export function EnvironmentAccessTab({environment}:{environment:Environment}) { const client=useQueryClient(); const [userId,setUserId]=useState(''); const {data:members=[]}=useQuery({queryKey:['environment-members',environment.id],queryFn:()=>api<Membership[]>(`/environments/${environment.id}/memberships`)}); const {data:users=[]}=useQuery({queryKey:['users-active'],queryFn:()=>api<User[]>('/users')}); const refresh=()=>client.invalidateQueries({queryKey:['environment-members',environment.id]}); async function add(){await api(`/environments/${environment.id}/memberships`,{method:'POST',body:JSON.stringify({user_id:userId,group_id:null})});setUserId('');await refresh()} async function remove(id:string){await api(`/environments/${environment.id}/memberships/${id}`,{method:'DELETE'});await refresh()} return <Stack spacing={2}>{members.map(m=><Paper key={m.id} variant="outlined" sx={{p:2}}><Stack direction="row" gap={1} alignItems="center"><Typography fontWeight={800} sx={{flex:1}}>{m.user_name}</Typography><Chip size="small" label={m.source==='rule'?'כלל אוטומטי':'ידני'}/><Button color="error" disabled={m.source==='rule'} onClick={()=>remove(m.id)}>הסרה</Button></Stack></Paper>)}<Paper variant="outlined" sx={{p:2}}><Stack direction={{xs:'column',sm:'row'}} gap={1}><TextField select fullWidth label="משתמש פעיל" value={userId} onChange={e=>setUserId(e.target.value)}>{users.map(u=><MenuItem key={u.id} value={u.id}>{u.display_name}</MenuItem>)}</TextField><Button variant="contained" disabled={!userId} onClick={add}>הוספת משתמש</Button></Stack></Paper></Stack> }
