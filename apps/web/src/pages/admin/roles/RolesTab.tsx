import{Add}from'@mui/icons-material';import{Box,Button,Chip,Paper,Stack,Typography}from'@mui/material';import type{Role}from'../../../types';export function RolesTab({roles,onCreate}:{roles:Role[];onCreate:()=>void}){return <Stack spacing={2}><Button sx={{alignSelf:'flex-start'}} variant="contained" startIcon={<Add/>} onClick={onCreate}>תפקיד חדש</Button>{roles.map(role=><Paper key={role.id} variant="outlined" sx={{p:2}}><Typography fontWeight={800}>{role.name}</Typography><Typography color="text.secondary">{role.description||role.code}</Typography><Box>{role.permissions.map(permission=><Chip key={permission} size="small" label={permission} sx={{m:.25}}/>)}</Box></Paper>)}</Stack>}
