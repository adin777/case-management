import { Chip, FormControl, InputLabel, MenuItem, Select, Stack } from '@mui/material';
import type { Environment } from '../../../types';
export function EnvironmentMultiSelect({ environments, value, onChange }: { environments: Environment[]; value: string[]; onChange: (value: string[]) => void }) { const active = environments.filter((item) => item.is_active); return <FormControl><InputLabel>סביבות רלוונטיות</InputLabel><Select multiple label="סביבות רלוונטיות" value={value} onChange={(event) => { const selected = typeof event.target.value === 'string' ? event.target.value.split(',') : event.target.value; onChange(selected.includes('__all__') ? active.map((item) => item.id) : selected); }} renderValue={(selected) => <Stack direction="row" gap={.5} flexWrap="wrap">{selected.map((id) => <Chip key={id} size="small" label={active.find((item) => item.id === id)?.name_he || id} />)}</Stack>}><MenuItem value="__all__">כל הסביבות</MenuItem>{active.map((item) => <MenuItem key={item.id} value={item.id}>{item.name_he}</MenuItem>)}</Select></FormControl>; }
