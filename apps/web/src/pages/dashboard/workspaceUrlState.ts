import type { WorkspaceFilters } from './types';
export function readWorkspaceState(search:URLSearchParams,defaults:WorkspaceFilters){const dynamic:Record<string,string>={};search.forEach((value,key)=>{if(key.startsWith('field_'))dynamic[key.slice(6)]=value});const filters={...defaults,dynamic};for(const key of ['search','created_from','created_to','title','updated_from','updated_to','environment_id'] as const){if(search.has(key))filters[key]=search.get(key)||''}const activity=search.get('activity_state');if(activity==='active'||activity==='inactive'||activity==='all')filters.activity_state=activity;filters.include_participating=search.get('include_participating')==='true';return {filters,page:Number(search.get('page')||1),pageSize:Number(search.get('page_size')||25),sort:search.get('sort')||'updated_at:desc'};}
export function writeWorkspaceState(current:URLSearchParams,filters:WorkspaceFilters,page:number,pageSize:number,sort:string){const next=new URLSearchParams();for(const key of ['tab','queue']){const value=current.get(key);if(value)next.set(key,value)}Object.entries(filters).forEach(([key,value])=>{if(key==='dynamic')Object.entries(value as Record<string,string>).forEach(([id,item])=>{if(item)next.set(`field_${id}`,item)});else if(value!==''&&value!==false)next.set(key,String(value))});next.set('page',String(page));next.set('page_size',String(pageSize));next.set('sort',sort);return next;}
