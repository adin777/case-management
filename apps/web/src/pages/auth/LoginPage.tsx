import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {Alert,Box,Button,Card,CardContent,Stack,TextField,Typography} from '@mui/material';import {api,token} from '../../api/client';
export function LoginPage(){const nav=useNavigate();const[email,setEmail]=useState('admin@example.com');const[password,setPassword]=useState('Admin123!');const[error,setError]=useState('');async function login(){try{const result=await api<{access_token:string;refresh_token:string}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});token.set(result.access_token,result.refresh_token);nav('/')}catch(e){setError((e as Error).message)}}return <Box className="login-art"><Card sx={{width:{xs:'92%',sm:460},p:2}}><CardContent><Stack spacing={3}><Typography variant="h4">מרכז השירות</Typography><Typography color="text.secondary">ניהול פניות ותהליכי עבודה במקום אחד</Typography>{error&&<Alert severity="error">{error}</Alert>}<TextField label="דוא״ל" value={email} onChange={e=>setEmail(e.target.value)}/><TextField label="סיסמה" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button size="large" variant="contained" onClick={login}>כניסה למערכת</Button></Stack></CardContent></Card></Box>}
