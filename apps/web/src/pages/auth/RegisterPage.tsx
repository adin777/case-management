import {FormEvent,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Alert,Box,Button,Card,CardContent,CircularProgress,Stack,TextField,Typography} from '@mui/material';import {ApiError,getCurrentUser,register,token} from '../../api/client';import {useQueryClient} from '@tanstack/react-query';
export function RegisterPage(){const nav=useNavigate();const qc=useQueryClient();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError('');if(password!==confirm){setError('הסיסמאות אינן תואמות');return}setLoading(true);try{const result=await register(name,email,password);token.set(result.access_token,result.refresh_token);const user=await getCurrentUser();qc.setQueryData(['me'],user);nav('/',{replace:true})}catch(caught){console.error('Registration failed',caught);setError(caught instanceof ApiError?caught.message:'ההרשמה נכשלה. פרטי השגיאה נרשמו בקונסול.')}finally{setLoading(false)}}return <Box className="login-art"><Card sx={{width:{xs:'92%',sm:500},p:2}}><CardContent><Box component="form" onSubmit={submit}><Stack spacing={2.5}><Typography variant="h4">הרשמה למערכת</Typography>{error&&<Alert severity="error">{error}</Alert>}<TextField label="שם מלא" value={name} onChange={e=>setName(e.target.value)} required/><TextField label="כתובת מייל" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><TextField label="סיסמה" type="password" value={password} onChange={e=>setPassword(e.target.value)} required helperText="לפחות 8 תווים, אות אחת ומספר אחד"/><TextField label="אימות סיסמה" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/><Button type="submit" variant="contained" size="large" disabled={loading}>{loading?<><CircularProgress size={20} color="inherit" sx={{ml:1}}/>נרשם...</>:'הרשמה'}</Button><Typography textAlign="center"><Link to="/login">חזרה לכניסה</Link></Typography></Stack></Box></CardContent></Card></Box>}
