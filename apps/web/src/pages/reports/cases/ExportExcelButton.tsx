import{Download}from'@mui/icons-material';import{Button}from'@mui/material';import{apiDownload}from'../../../api/client';import type{ReportFilters}from'./CaseReportFilters';
export function ExportExcelButton({filters}:{filters:ReportFilters}){async function download(){const query=new URLSearchParams(Object.entries(filters).filter(([,v])=>v));const blob=await apiDownload(`/reports/cases/export?${query}`);const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download='case-report.xlsx';document.body.appendChild(link);link.click();window.setTimeout(()=>{link.remove();URL.revokeObjectURL(url)},1000)}return <Button variant="contained" startIcon={<Download/>} onClick={download}>ייצוא ל-Excel</Button>}
